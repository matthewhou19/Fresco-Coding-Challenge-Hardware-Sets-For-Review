# Serving the extraction results needs four packages -- not the extractor's
# own stack (pdfplumber, anthropic): the pipeline runs offline and the server
# only reads its JSONL output and renders pages.
fastapi==0.141.1
uvicorn[standard]==0.52.3
pypdfium2==5.13.0
pillow==12.3.0
